import type { MenuItem } from '@/interfaces/MenuInterface';

export const menuData: MenuItem[] = [
  { label: 'Home', path: '/' },
  { label: 'Profile', path: '/profile' },
  { label: 'About', path: '/about' },
  // ... agregar otras vistas según sea necesario
];